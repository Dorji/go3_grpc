# Определяем базовый образ для построения приложения
FROM golang:latest 

# Создаем директорию /app
RUN mkdir /app

#Дублирование всей начинки директории (Lec2)
ADD . /add

# Выбираем рабочую директорию
WORKDIR /app

# Компилируем go код (-o <name> - создать исполняемый файл с названием <name>)
RUN go build -o main .

# Запускаем код
CMD ["/app/main"]